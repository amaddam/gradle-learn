server:
  profiles:
    active: dev
  port: 7777

spring:
  datasource:
    username: postgres
    password: zQy2iQp5
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/gradle-microservice-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  application:
    name: service-user